directories:
    - ${ROOT_DIR}/test/tests
specs:
    - tests
dependencies:
    - framework
repos:
    default: https://github.com/pvmartins87/swiftlet (push)
reports: !include ${ROOT_DIR}/doc/sqa_reports.yml
